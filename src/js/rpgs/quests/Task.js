"use strict";
import BaseObject from '../core/BaseObject';

let Task = (function(){
  return class Task extends BaseObject {
    constructor(data) {
      super(data);
    }

    //to do
  };
})();

module.exports = Task;
