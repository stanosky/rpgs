"use strict";
import BaseNode from '../core/BaseNode';

let Task = (function(){
  return class Task extends BaseNode {
    constructor(data) {
      super(data);
    }

    //to do
  };
})();

module.exports = Task;
